coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 1%
        only_pulls: true
        if_ci_failed: error

    patch:
      default:
        target: 80%
        threshold: 1%
comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false